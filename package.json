{
  "name": "prepwise-meal-prep",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check client.js && node --check subscription.js && node --check native-store.js && node --check app-store-notifications.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
